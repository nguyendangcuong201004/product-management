extends ../../layouts/default.pug
include ../../mixins/box-head.pug 

block main 

    .container.my-5

        include ../../partials/list-btn-user.pug

        .section-head(style="margin-top: 24px")
            div 
                h2.section-title Lời mời đã gửi

        .row 
            each user in users
                .col-6 
                    div(class="box-user add")
                        div(class="inner-avatar")
                            img(src="https://i.imgur.com/ttyjQxs.jpeg", alt="avatat")
                        div(class="inner-info")
                            div(class="inner-name") #{user.fullName}
                            div(class="inner-buttons")
                                button(
                                    class="btn btn-sm btn-primary mr-1 add"
                                    btn-add-friend=user.id
                                ) Kết bạn
                                button(
                                    class="btn btn-sm btn-secondary mr-1"
                                    btn-cancel-friend=user.id
                                ) Hủy 

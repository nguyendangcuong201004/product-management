extends ../../layouts/default.pug
include ../../mixins/alert.pug
include ../../mixins/box-head.pug

block main 
    +alert-success()
    +alert-error()

    .container.my-3 
        .row 
            .col-12 
                +box-head("Đặt hàng thành công")
        .row 
            .col-12 
                div(class="alert alert-success" style="font-size: 18px") 
                    | Chúc mừng bạn đã đặt hàng thành công! Chúng tôi sẽ xử lý 
                    | đơn hàng của bạn trong thời gian sớm nhất.
        .row    
            .col-12 
                h4(class="mb-2") Thông tin đơn hàng
                table(class="table table-bordered table-hover")
                    thead 
                        tr 
                            th STT
                            th Ảnh
                            th Tên
                            th Giá 
                            th Số lượng
                            th Tổng tiền 
                    tbody   
                        if(order.products.length > 0)
                            each item, index in order.products
                                tr  
                                    td  #{index + 1}
                                    td  
                                        img(src=item.thumbnail, alt=item.title width="100px")
                                    td  #{item.title}
                                    td  #{item.priceNew}$
                                    td  #{item.quantity}
                                    td  #{item.totalPrice}$
                if(order.products.length > 0)
                    h3(class="text-right") Tổng đơn hàng: #{order.orderTotal}$
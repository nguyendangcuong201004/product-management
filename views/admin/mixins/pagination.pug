mixin pagination(objectPagination)
    div
        ul(class="pagination")
            li(class="page-item")
                if (objectPagination.currentPage > 1)
                    button(class="page-link" button-pagination=objectPagination.currentPage-1) Trang trước
            -   for (var i = 1; i <= objectPagination.totalPage; i++)
                if (i == objectPagination.currentPage)
                    li(class="page-item active")
                        button(class="page-link" button-pagination=i) #{i}
                else 
                    li(class="page-item")
                        button(class="page-link" button-pagination=i) #{i}
            li(class="page-item")
                if (objectPagination.currentPage < objectPagination.totalPage)
                    button(class="page-link" button-pagination=+objectPagination.currentPage+1) Trang sau 
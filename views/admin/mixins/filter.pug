mixin filter(filterStatus)
    .seg-group(role="group" aria-label="Lọc theo trạng thái")
        each item in filterStatus
            button(
                type="button"
                class=`seg-item ${item.class}`
                button-status=item.status
            ) #{item.name}

mixin search(keyword)
    form(id="form-search" class="search-field")
        i(class="fa-solid fa-magnifying-glass search-icon")
        input(
            type="text"
            placeholder="Tìm sản phẩm…"
            name="keyword"
            class="form-control"
            value=keyword
        )

extends ../../layouts/default.pug
include ../../mixins/alert.pug 

block main 
    .page-header 
        .page-header-info 
            h1.page-header-title Thêm mới tài khoản
            p.page-header-desc Tạo tài khoản quản trị và gán nhóm quyền
        .page-header-actions 
            a(href=`${prefixAdmin}/accounts` class="btn btn-ghost")
                i.fa-solid.fa-arrow-left
                | Danh sách

    +alert-success()
    +alert-error()

    form(
        action=`${prefixAdmin}/accounts/create`
        method="POST"
        enctype="multipart/form-data"
    )
        .panel.form-panel 

            .form-section 
                p.form-section-title Thông tin tài khoản
                .form-grid.cols-2 
                    div 
                        label(for="fullName") Họ và tên
                        input(
                            class="form-control"
                            id="fullName"
                            name="fullName"
                            required
                            type="text"
                        ) 
                    div 
                        label(for="email") Email
                        input(
                            class="form-control"
                            id="email"
                            name="email"
                            required
                            type="email"
                        ) 
                    div 
                        label(for="password") Mật khẩu
                        input(
                            class="form-control"
                            id="password"
                            name="password"
                            required
                            type="password"
                        ) 
                    div 
                        label(for="phone") Số điện thoại
                        input(
                            class="form-control"
                            id="phone"
                            name="phone"
                            type="text"
                        ) 

            .form-section 
                p.form-section-title Ảnh đại diện
                .form-grid 
                    div.upload-field(upload-image)
                        label(for="avatar") Chọn ảnh
                        input(
                            type="file"
                            class="form-control-file"
                            id="avatar"
                            name="avatar"
                            accept="image/*"
                            upload-image-input
                        ) 
                        img(
                            src=""
                            class="image-preview"
                            upload-image-preview
                            alt="Avatar preview"
                        )

            .form-section 
                p.form-section-title Phân quyền và trạng thái
                .form-grid.cols-2 
                    div 
                        label(for="role_id") Nhóm quyền  
                        select(
                            name="role_id"
                            id="role_id"
                            class="form-control"
                        ) 
                            option(disabled value) -- Chọn nhóm quyền -- 
                            each role in roles 
                                option(value=role.id) #{role.title}
                    div 
                        label Trạng thái
                        .radio-group 
                            div(class="form-check form-check-inline")
                                input(
                                    type="radio"
                                    class="form-check-input"
                                    id="statusActive"
                                    name="status"
                                    value="active"
                                    checked
                                )
                                label(for="statusActive" class="form-check-label") Hoạt động 
                            div(class="form-check form-check-inline")
                                input(
                                    type="radio"
                                    class="form-check-input"
                                    id="statusInActive"
                                    name="status"
                                    value="inactive"
                                )
                                label(for="statusInActive" class="form-check-label") Ngừng hoạt động 

            .form-actions 
                button(
                    class="btn btn-primary"
                    type="submit"
                ) Tạo mới 
                a(href=`${prefixAdmin}/accounts` class="btn btn-ghost") Hủy
